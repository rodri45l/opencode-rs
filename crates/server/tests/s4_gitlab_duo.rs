//! Port of packages/opencode/test/provider/gitlab-duo.test.ts (upstream 18ef3cc).
//! Behaviour pinned by src/provider/provider.ts; see docs/TEST-PORT.md.
//!
//! Status: `n/a` — the upstream file is entirely commented out (`export {}`) with
//! a "TODO: UNCOMMENT WHEN GITLAB SUPPORT IS COMPLETED" marker, so there are no
//! live behavioural assertions to port. Kept as a placeholder so the reference
//! path maps to a Rust test file when GitLab support lands.
