//! Port of packages/tui/test/plugin/slots.test.tsx (upstream 18ef3cc).
//! n/a (visual — human-verified): asserts that a replacement plugin slot mounts
//! its component once under the OpenTUI renderer.
